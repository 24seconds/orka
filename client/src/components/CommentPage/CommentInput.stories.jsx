import React from "react";
import CommentInputComponent from "./CommentInputComponent";

export default {
    title: "Orka/Comment/Input",
    comment: CommentInputComponent,
};

const Template = () => <CommentInputComponent />;

export const Input = Template.bind();
